```
# StellarStream Backend — Environment Variables
# Copy this file to .env and fill in your values.
# ALL variables are required — the server will exit on startup if any are missing or invalid.

# ─── Database ────────────────────────────────────────────────────────────────
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:password@localhost:5432/stellarstream

# ─── Stellar / Soroban ───────────────────────────────────────────────────────
# Soroban RPC endpoint for your target network
# Testnet: https://soroban-testnet.stellar.org
# Mainnet: https://soroban-mainnet.stellar.org
STELLAR_RPC_URL=https://soroban-testnet.stellar.org

# Deployed StellarStream contract address (56 chars, starts with 'C')
CONTRACT_ID=CXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Network passphrase — must match the network above
# Testnet: Test SDF Network ; September 2015
# Mainnet: Public Global Stellar Network ; September 2015
NETWORK_PASSPHRASE=Test SDF Network ; September 2015